{
  "name": "orion",
  "version": "1.0.0",
  "private": true,
  "description": "Lunaria — Stremio-protocol catalog client that streams into VLC or mpv",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "wait-on http://localhost:5173 && cross-env ELECTRON_START_URL=http://localhost:5173 npm run start --prefix electron",
    "dev": "concurrently -k -n vite,electron -c cyan,magenta \"npm:dev:frontend\" \"npm:dev:electron\"",
    "build:frontend": "cd frontend && npm run build",
    "start": "npm run build:frontend && npm run start --prefix electron",
    "install:frontend": "cd frontend && npm install",
    "install:electron": "cd electron && npm install",
    "install:all": "npm run install:frontend && npm run install:electron"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "wait-on": "^8.0.2"
  }
}
